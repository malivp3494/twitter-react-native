import React, { Component } from 'react';
import { Root, Text } from '../../components';

class CreateTweet extends Component {
  state = {};
  render() {
    return (
      <Root>
        <Text>Create a new Tweet</Text>
      </Root>
    );
  }
}

export default CreateTweet;
