import React, { Component } from 'react';
import { Root, Text } from '../../components';

class TwitterAds extends Component {
  state = {};
  render() {
    return (
      <Root>
        <Text>TwitterAds</Text>
      </Root>
    );
  }
}

export default TwitterAds;
